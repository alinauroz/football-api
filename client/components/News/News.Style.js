import { StyleSheet } from 'react-native'

const styles = {
    highlight: {
        fontWeight: 'bold',
        color: '#333'
    },
    date: {
        fontStyle: 'italic',
        color: '#333333aa'
    }
}

export default StyleSheet.create(styles);