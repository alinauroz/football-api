import React from 'react' 
import {
    Text,
    View
} from 'react-native'
import { Button } from 'react-native-elements';

const Login = (props) => {
    return (
        <Button
          title="Solid Button"
        />
    )
}

export default Login;